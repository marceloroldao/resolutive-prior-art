# RT-0002 — Information-Event Time and Recurrent Causal Trajectory Compression

**Identifier:** RT-0002  
**Status:** EXPERIMENTAL  
**Version:** 0.3  
**Disclosure date:** 2026-09-05  
**Author:** Marcelo Roldão Matos

## Purpose

This disclosure records a trajectory-generator result that separates physical transition time from entropy-bearing information-event time inside a recurrent causal state machine and includes an executable variable-length macro-event rank/unrank codec plus exact microscopic trajectory regeneration for the documented recurrent core.

## Mechanism

A deterministic reversible/admissible trajectory system may contain long stretches of transitions that are forced by public dynamics and a much smaller set of branch events where multiple admissible successors exist.

The recurrent causal graph can therefore be reduced to a macrograph whose edges represent deterministic flights between branch states.

Representative public result:

```text
A -> B : 1 physical step
A -> A : 12 physical steps
B -> A : 2 physical steps
B -> A : 5 physical steps
```

The macrograph preserves the asymptotic trajectory growth factor while exposing a second clock:

- `t`: physical transition count;
- `k`: count of entropy-bearing branch events.

## Information-rate relation

For the public recurrent-core experiment, the dominant growth factor satisfies

```text
lambda^12 - lambda^9 - lambda^6 - 1 = 0
lambda ~= 1.206189700118
log2(lambda) ~= 0.270456820917 bit / physical step
```

Under the tested maximal-entropy path measure, a branch event spans about 2.59856 physical steps on average and carries about 0.70280 bit, whose ratio recovers the same physical-time information rate.

## Operational macro-event codec

The repository contains:

- `trajectory_generator/information_clock_codec.py`;
- `tests/test_information_clock_codec.py`.

The codec treats a valid macro-event path as a member of the finite set of admissible paths with a fixed public physical length. It provides:

```text
count_paths(start_state, total_length, optional_end_state)
rank_path(event_labels, start_state, total_length, optional_end_state)
unrank_path(rank, start_state, total_length, optional_end_state)
regenerate_physical_lengths(event_labels, start_state)
```

For a fixed start branch state, public physical length, optional end state, and rank, `unrank_path` deterministically reconstructs the unique macro-event sequence. `rank_path` maps that sequence back to the same integer.

## Exact microscopic regeneration

The repository now also contains:

- `experiments/information_clock_physical_roundtrip.py`;
- `tests/test_information_clock_physical_roundtrip.py`.

The physical round-trip layer re-derives the concrete branch states and every deterministic flight directly from the public recurrent causal graph. It does not store a microscopic path table in the address.

The demonstrated transformation is:

```text
microscopic recurrent-core trajectory
    -> canonical macro-event sequence
    -> rank/address

then discard the trajectory and macro-event sequence

(rank/address, public physical length, public start branch)
    -> unrank macro-event sequence
    -> re-derive deterministic flights from public dynamics
    -> identical microscopic node-by-node trajectory
```

The physical nodes are phase-lifted causal states of the form:

```text
(history3, topology3, phase3)
```

Consecutive macro-events share their branch endpoint/startpoint; expansion therefore concatenates the public deterministic flight paths without duplicating shared branch nodes.

The committed test exhausts every admissible macro path for both start branch states over physical lengths 1 through 36 and verifies:

1. macrograph labels derived from the physical graph equal the canonical codec labels;
2. each public macro-edge path length equals its declared physical length;
3. `rank -> unrank` recovers the identical macro-event sequence;
4. expansion of the recovered sequence regenerates the identical microscopic causal-node trajectory;
5. regenerated microscopic length equals the requested public physical length.

This closes the previously open bridge from information-event rank/unrank to node-by-node physical replay for the documented dominant recurrent core.

## Evidence baseline

Public repository: `marceloroldao/trajectory.generator`.

Relevant evidence includes:

- `experiments/recurrent_orbit_core.py`;
- `experiments/information_clock_macrograph.py`;
- `trajectory_generator/information_clock_codec.py`;
- `tests/test_information_clock_codec.py`;
- `experiments/information_clock_physical_roundtrip.py`;
- `tests/test_information_clock_physical_roundtrip.py`;
- `docs/recurrent_orbit_core_2026-09-02.md`;
- `docs/information_clock_macrograph_2026-09-02.md`.

Reference commits:

- `d84e783585d5bbecdd1b607807625e633bf14237` — macro-event rank/unrank codec;
- `71219ef22cefa5ba53534dda6c861850bfd85cb7` — macro-event round-trip tests;
- `cd776acd99b803ba6da57d6b865daf12c07a04cb` — microscopic physical round-trip experiment;
- `d1e2f8133c97380433ac8c4065467b55f5e30da8` — exhaustive microscopic regeneration tests.

The repository also records the fundamental information bound that an arbitrary `n`-bit family cannot be injectively addressed by a fixed `w`-bit final state for `n > w` unless the admissible family is sufficiently constrained or extra information is supplied.

## Evidence qualification

At the time this disclosure was updated, the GitHub connector did not expose a status/check run for commit `d1e2f813...`. Therefore the current evidence classification remains **implementation + committed exhaustive tests**, not independently confirmed CI-green execution through this interface.

The present test window is bounded to physical lengths 1..36 for tractability. The mechanism itself is defined recursively for longer admissible paths, but a broader execution campaign and independent reproducibility run remain desirable before changing status from EXPERIMENTAL.

## Scope and limitations

This disclosure does not claim lossless compression of arbitrary data beyond information-theoretic limits. It concerns exact representation of constrained admissible trajectory families whose entropy is lower than the raw physical-step sequence length.

The demonstrated address requires public side information identifying the physical length and start branch state. It therefore must not be described as recovery from a fixed-size hash alone.

It also does not claim invention of event time, semi-Markov processes, causal-state models, entropy rate, ranking of combinatorial languages, or variable-length coding in isolation. The disclosed mechanism is their specific use in a public recurrent causal trajectory macrograph where deterministic flights are collapsed, entropy-bearing branch events are ranked, and the complete microscopic recurrent-core trajectory is regenerated by re-expanding public deterministic dynamics.
