# RM-0003 — Persistent Concept Identity and Deterministic Concept-Relation Resolution

**Identifier:** RM-0003  
**Status:** EXPERIMENTAL  
**Version:** 0.2  
**Disclosure date:** 2026-09-05  
**Author:** Marcelo Roldão Matos

## Purpose

This disclosure records a post-v1 Memoria.ia mechanism in which durable concept identities, aliases, contextual senses and relation paths are persisted independently of transient language-model context and can be materialized into a native deterministic resolution path.

## Mechanism

The system separates at least four concerns:

1. authoritative concept identity and relation state;
2. durable persistence of that state;
3. native materialization into an execution index;
4. bounded deterministic resolution after ordinary memory resolution misses.

A concept is not identified only by the surface text used by a user. Multiple aliases may converge to a stable concept identity, while ambiguous forms fail closed unless sufficient contextual evidence selects a valid sense.

Concept relations can be traversed over bounded paths with provenance retained for audit. Native and Python paths are required to maintain behavioral parity before native activation.

## Resolution order

```text
ordinary memory resolution
    -> HIT: return
    -> MISS: explicit concept/alias resolution
             -> bounded relation path resolution
             -> HIT or UNRESOLVED
```

Ambiguous or unsupported interpretation returns `UNRESOLVED`; the resolver does not invent a concept merge.

## Persistence and native materialization

```text
authoritative concept state
 -> deterministic catalog export
 -> stable fingerprint
 -> native/BDR materialization
 -> restart-safe reload
 -> native resolver activation
```

The authoritative semantic record remains distinct from the derived native execution index.

## Frozen implementation evidence

Public repository: `marceloroldao/memoria.ia`.

Reference commits:

- `e086ba9647bb00b85195a49b87dd5dba8feac896` — pure-C concept identity kernel with explicit IDs, alias normalization, namespace isolation and fail-closed contextual polysemy;
- `1224f407079c1aef70769ba5c59528bba49fabbb` — native concept state round-trip preserving aliases, contextual cues and namespace isolation;
- `bdc5260fa06d1f8e4afac1e4603ac8a4a2779b53` — durable native concept state persisted in Resolutive-DB with restart coverage;
- `e0dc57ede886ba59deb83d77b715439fb522cb82` — persisted concept state attached to native runtime lifecycle with host restart behavior;
- `3ef70df8ff1bc12a551ce46020f8020b6bd59eaa` — native concept query-rewrite kernel;
- `c8420b80ac958adf78f3a965414a9ae04be7bb58` — direct C-vs-Python parity gate, including Portuguese, multiword, canonical, unknown and contextual-polysemy vectors;
- `229c9dfb6ddf1b0bd3f4a2195d4ed55c942bc715` — deterministic authoritative catalog export with stable fingerprints;
- `7db7bda0c731c0282eb35476c87d15f02598ba4a` — authoritative catalog materialization into native BDR;
- `6d2920f696ced4b238bd22c130d9641758ac1444` — product-startup materialization;
- `501ab81ce93c897e6addb55a475aa9b51901369b` — miss-only native concept resolver activation;
- `a22f7751339db0762f0ffb120a0dd0b26d860554` — persisted concept materializes into native HTTP resolution end-to-end;
- `4f9ac9440c56855266ab3d1acd853b0706dee646` — merge of the end-to-end persisted-concept/native-resolve proof.

The evidence chain therefore covers identity -> serialization -> durable BDR persistence -> restart lifecycle -> native/Python parity -> deterministic materialization -> miss-only activation -> HTTP product resolution.

## Prior-art narrowing

External prior-art review found strong antecedents for stable symbolic identifiers, aliases, concept graphs, contextual disambiguation and indexed lookup. Those primitives are not claimed in isolation.

The narrower disclosed combination is:

```text
authoritative persistent concept state
 -> deterministic derived native index
 -> restart reconstruction
 -> fail-closed contextual ambiguity
 -> explicit HIT/UNRESOLVED semantics
 -> native concept path only after ordinary memory MISS
```

## Scope and limitations

No claim is made that arbitrary natural-language understanding is deterministic or constant-time. The disclosure does not claim invention of symbolic IDs, aliases, databases, graph traversal, contextual disambiguation or hash indexing individually.

Status remains `EXPERIMENTAL`: the commits provide a public implementation/evidence chain, but this registry does not elevate the mechanism to `VALIDATED` without a separately frozen reproducibility artifact and defined acceptance criteria.
